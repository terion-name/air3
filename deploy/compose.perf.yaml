services:
  edge-gateway:
    cpus: 1.0

  private-connector:
    cpus: 1.0

  versitygw:
    networks:
      - private
      - perf-host
    ports:
      - "${AIR3_PERF_S3_PORT:-10000}:10000"

networks:
  perf-host: {}
